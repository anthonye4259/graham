workflows:
  ios-capacitor:
    name: iOS App Store Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: "codemagic"
    environment:
      node: 22
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Web App
        script: npm run build
      - name: Sync Capacitor iOS
        script: npx cap sync ios
      - name: Fetch code signing files and set up Xcode
        script: |
          app-store-connect fetch-signing-files "com.graham.ai" --type IOS_APP_STORE --create
          xcode-project use-profiles
      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --project "ios/App/App.xcodeproj" \
            --scheme "App"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration # Ensure Apple Developer Portal is connected in Codemagic User settings
        submit_to_testflight: true
